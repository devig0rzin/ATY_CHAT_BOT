# Prompts

These Markdown files are the human-editable source of truth for assistant prompts.

Cloudflare Workers do not read local files at runtime. Run `npm run build:prompts` to generate `src/generated/prompts.ts` before development, checking, or deployment.
